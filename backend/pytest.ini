[pytest]
# Tests import the 'app' package from the backend/ root, so add rootdir to sys.path
# and the plain 'pytest' command works without needing 'python -m pytest'.
pythonpath = .
testpaths = tests
